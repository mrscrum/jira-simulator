# Detailed Contract Drafts

These files are optional design notes from an earlier, more detailed planning pass. Pavel requested
that the active plan remain high level, so implementations are not required to preserve these exact
schemas, algorithms, endpoint shapes, or constants.

Use [`../high-level-plan.md`](../high-level-plan.md) for product requirements. Reuse a contract here
only when it helps the current milestone, and adapt it through normal test-first implementation and
the documentation/assumption process in `/AGENTS.md`.
